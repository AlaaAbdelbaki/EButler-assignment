import 'package:flutter/material.dart';

class ChatMobile extends StatelessWidget {
  const ChatMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Chat mobile'));
  }
}
